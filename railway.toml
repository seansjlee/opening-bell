[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.dashboard.server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "web"
